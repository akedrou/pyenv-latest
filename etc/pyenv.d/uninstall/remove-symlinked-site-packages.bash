PYENV_LATEST_ROOT=${PYENV_LATEST_ROOT:-"$PYENV_ROOT/plugins/pyenv-latest"}

after_uninstall "remove_site_packages"

remove_site_packages() {
    if [ -d "${PYENV_LATEST_ROOT}/site-packages/${VERSION_NAME}" ]; then
      rm -rf "${PYENV_LATEST_ROOT}/site-packages/${VERSION_NAME}"
    fi
}
